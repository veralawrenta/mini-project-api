<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Reset your password</title>
    <style>
      body { font-family: system-ui, -apple-system, "Segoe UI", Roboto,
      "Helvetica Neue", Arial; margin:0; padding:0; background:#f5f7fa; }
      .container { max-width:600px; margin:24px auto; background:#ffffff;
      border-radius:8px; padding:24px; box-shadow:0 6px 18px rgba(0,0,0,0.06); }
      .brand { font-weight:700; font-size:18px; margin-bottom:12px;
      color:#111827; } p { color:#374151; line-height:1.5; margin:16px 0; } .btn
      { display:inline-block; text-decoration:none; padding:12px 20px;
      border-radius:6px; background:#2563eb; color:#ffffff; font-weight:600; }
      .small { font-size:13px; color:#6b7280; margin-top:12px; } .footer {
      font-size:12px; color:#9ca3af; margin-top:20px; text-align:center; }
      a.fallback { color:#2563eb; word-break:break-all; }
    </style>
  </head>
  <body>
    <div class="container" role="article" aria-label="Password reset">
      <div class="brand">YourApp</div>

      <h2 style="margin:0 0 8px 0; color:#111827;">Reset your password</h2>

      <p>We received a request to reset your password. Click the button below to
        create a new one.</p>

      <p style="text-align:center; margin:24px 0;">
        <a href="{{link}}" class="btn" target="_blank" rel="noopener">Reset
          password</a>
      </p>

      <p class="small">If the button doesn't work, copy and paste the link below
        into your browser:</p>
      <p class="small"><a
          href="{{link}}"
          class="fallback"
          target="_blank"
          rel="noopener"
        >{{link}}</a></p>

      <p class="small">This link will expire in 1 hour. If you didn't request a
        password reset, you can safely ignore this email.</p>

      <div class="footer">
        &mdash; The YourApp team
      </div>
    </div>
  </body>
</html>